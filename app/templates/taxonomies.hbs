<h2>Taxonomies</h2>

<div class="card table-card">
  {{#each taxonomy in model}}
    <div data-id="">
      {{taxonomy.name}}
    </div>
  {{/each}}
</div>

<div class="button-bar">
  <button>New Taxonomy</button>
</div>

{{outlet}}
