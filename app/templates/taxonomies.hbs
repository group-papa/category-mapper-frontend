<div class="container with-sidebar">
  <div class="card-table float-left">
    <div class="cards">
      {{#each taxonomy in model}}
        {{#list-item delete="delete" item=taxonomy}}
          {{taxonomy.name}}
        {{/list-item}}
      {{/each}}
    </div>
    <div class="actions align-center">
      {{input value=uploadName class='large-input'}}
      {{file-input action="upload" text="Upload Taxonomy"}}
    </div>
  </div>
  <div class="sidebar float-right">
  </div>
</div>

{{outlet}}
